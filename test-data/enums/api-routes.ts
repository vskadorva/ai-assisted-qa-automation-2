/** Repeated API paths — single source of truth for request helpers and route mocks. */
export enum ApiRoutes {
  Programs = "/api/programs",
  ProgramById = "/api/programs/:id",
  AuthLogin = "/api/auth/login",
}
