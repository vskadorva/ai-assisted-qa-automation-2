---
description: How to drive a QA ticket, backlog queue, or coverage gap to done by delegating to the test-writer, triage, self-heal, and bug-reporter subagents.
alwaysApply: true
---

# QA orchestration policy

You coordinate the QA subagents. Do the analysis yourself; delegate the rest.
Follow **Agent governance** in `constitution.mdc` (Proposal block + confidence gate) before substantive edits.

## Skills

- **jira-ticket-to-gherkin** — ticket given → turn acceptance criteria into a
  Gherkin test plan (you run this; do not delegate analysis).
- **explore-and-generate** — no ticket → map `tests/`, crawl the UI via
  Agent-Browser (a11y tree), diff coverage gaps, pick one highest-value
  uncovered flow, output a Gherkin plan for the test-writer. Read-only
  exploration. Do **not** use when a ticket already exists.
- **self-heal** — after triage classifies **test issue (drift)**, re-discover
  the element via Agent-Browser (a11y tree), patch the POM with a minimal
  role-based diff, re-run to prove green with assertions unchanged, and open
  a repair PR. Never for a real app bug; never touch spec assertions.

## Routing

- ticket given → analyze (`jira-ticket-to-gherkin`) → produce a test plan
- no ticket / "find what we're not testing" / "explore \<page\> for
  untested flows" / "expand coverage" → **explore-and-generate** → plan
- plan ready → delegate to **test-writer**
- spec returned → run: `npx playwright test`
- run is red → delegate to **triage** → follow **Heal on red** below
- backlog / queue run → follow **Backlog mode** below

## Heal on red

On a red run, delegate to **triage** to classify. Then route by classification:

**Drift (test issue)** — apply **self-heal** (you run the skill; do not
delegate POM edits to test-writer):

1. Re-discover the failing element via Agent-Browser (a11y tree).
2. Patch the POM — minimal role-based diff only; spec assertions unchanged.
3. Re-run to prove green with assertions unchanged.
4. Open a **repair PR** linked to the failing CI run and triage diagnosis.

**Real app bug** — delegate to **bug-reporter** to file and link the Jira
ticket. **Never heal a bug.**

**Inconclusive** — stop and escalate; do not heal or file until a human
decides.

### Heal budget and stop rules

- At most **5 heals per run** (one locator repair per heal).
- Stop and escalate if a heal will not prove green without weakening an
  assertion — never heal an assertion (see Refusals in
  `playwright-conventions.mdc` and the `assertion-guard` hook).
- Never merge a repair PR without a human.

## Backlog mode

Work a queue instead of one ticket. The agent never decides when to start —
something outside the agent starts the run (a Cursor Automation, a headless
`agent -p` run, or a local batch).

### Queue

All DS tickets in status **In Progress** that do **not** already have the
`tests-generated` label.

### Per ticket

1. Analyze → produce a test plan (`jira-ticket-to-gherkin`)
2. Delegate to **test-writer**
3. Run: `npx playwright test`
4. On red → delegate to **triage** (then **self-heal**, **bug-reporter**, or
   escalate per **Heal on red**)

### Output

- One PR per ticket
- Link the ticket on the PR
- Add the `tests-generated` label so the next run skips it

### Budget and stop rules

- At most **2 tickets per run**
- On a repeated identical failure: stop that ticket and move on to the next
- Never merge or close without a human

## Loop guard

- At most 3 delegations per task
- Never re-run an identical failing spec more than twice — then stop and ask
  (in Backlog mode: stop that ticket and move on)

## Guardrails

- A human approves every merge and every filed bug.
- No subagent edits application source — only the coordinator (or a human) may
  change app code / POMs outside the allowed blast radius. The coordinator
  applies **self-heal** for POM locator drift after triage confirms drift.
- Blast radius by specialist:
  - **test-writer** — write under `tests/` only; never application source, `pages/`, or other repo paths.
  - **triage** — read-only diagnosis; never edit source, push, merge, or apply fixes.
  - **bug-reporter** — read-only wrt the repo; files Jira only after human confirmation of a real app bug.

## Done

The suite is green for the ticket (or a bug is filed and linked); in Backlog
mode, each processed ticket has a PR, a ticket link, and the
`tests-generated` label — merge/close only by a human.

After an orchestrator run completes (CI test-generation workflow or local agent
session), `eval-report.md` is regenerated at the repo root. The coordinator
should read **Top reliability risk** and **Next action** before closing out.
